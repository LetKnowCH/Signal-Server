name: Build and Push Docker Image

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2

      - name: Set up JDK
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # 5.2.0
        with:
          distribution: 'temurin'
          java-version-file: .java-version
          cache: 'maven'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@99214aa6889fcddfa57764031d71add364327e59 # 6.1.3
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Get CodeArtifact Token
        id: get-token
        env:
          CA_DOMAIN: ${{ vars.CA_DOMAIN }}
          AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
          AWS_REGION: ${{ vars.AWS_REGION }}
        run: |
          TOKEN=$(aws codeartifact get-authorization-token \
            --domain "$CA_DOMAIN" \
            --domain-owner "$AWS_ACCOUNT_ID" \
            --region "$AWS_REGION" \
            --query authorizationToken \
            --output text)
          
          echo "::add-mask::$TOKEN"
          echo "token=$TOKEN" >> "$GITHUB_OUTPUT"

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@fa648b43de3d4d023bcb3f89ed6940096949c419 # 2.1.5

      # service/pom.xml reads service/config/deploy.properties in the deploy phase; the file is gitignored
      # and the image checksums already live in the root pom, so an empty file is enough.
      - name: Create deploy.properties
        run: touch service/config/deploy.properties

      - name: Build and push container image
        env:
          CODEARTIFACT_AUTH_TOKEN: ${{ steps.get-token.outputs.token }}
        run: |
          ./mvnw -s settings.xml -B deploy \
            -Pexclude-spam-filter \
            -DskipTests \
            -Dproperties.skip=true \
            -Djib.to.image="${{ steps.login-ecr.outputs.registry }}/${{ vars.ECR_REPOSITORY }}:${{ github.sha }}" \
            ${{ github.ref_type == 'tag' && format('-Djib.to.tags={0}', github.ref_name) || '' }}
